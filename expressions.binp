$ Operators && and || have the same precedence
$ so this will make a right leaning tree
$                 true && (false || true)
var bool testBool = True
var bool myBool = true &&  false || testBool
output myBool