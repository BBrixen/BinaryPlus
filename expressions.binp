$ Operators && and || have the same precedence
$ so this will make a right leaning tree
$ true && false || true --> true && (false || true)

$ Doesn't work yet still need to fix tree structure
$ var bool myBool = !(true && false) && true
$ var bool myBool = !true || false
$ output myBool


var int myInt = 1 + 2 * 5
var int myInt2 = (1 + 2) * 5

output 1 + 2 * 5 = myInt
output (1 + 2) * 5 = myInt2