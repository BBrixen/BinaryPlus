var int func add = (int x, int y) =>
    var int sum = x + y
    output returning sum from add
    return sum
end add

var int func mul = (int x, int y) =>
    return x * y
end mul

var int start = 3
var int x = 5 + 6 - (2*3)
output 'x' is x

$ functions within functions
$ and outer functions that are not part of the others all work
var int y = x * add(x, add(add(1, 2), add(1, 1))) + mul(x, start)
output x * add(x, add(start, 2)) + mul(x, start) = y

$ testing raw function calls, making sure thesea re still evaluated
add(mul(1, 1), 2)