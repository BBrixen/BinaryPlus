var bool condition = True

while (condition) =>
    output 'input 1 to terminate'
    var bool condition = input

    var bool func swapCond = (bool x) =>
        if (x) =>
            return False
        else =>
            return True
        end
    end swapCond

    output swapping condition
    var bool condition = swapCond(condition)
    output swapped to condition
    output

    $ the => after else is not actually enforced, yet
    else =>
        output this is the last loop
end

output 'terminated'
