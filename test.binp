var int func add = (int x, int y) =>
return x + y
end add

var int func mul = (int x, int y) =>
return x * y
end mul

var int start = 3
var int x = 5 + 6
var int y = x * add(x, add(add(1, 2), add(1, 1))) + mul(x, start)

output x * add(x, add(start, 2)) + mul(x, start) = y